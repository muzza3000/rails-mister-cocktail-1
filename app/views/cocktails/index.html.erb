<div class="container">
  <div class="row">
    <div class="logo">
    </div>
      <%= simple_form_for(SearchForm.new, url: cocktails_path, method: :get) do |f| %>
      <%= f.input :search_term, label: false, :input_html => { :class => 'search_field' }, placeholder: 'Search', hint: 'Search for cocktails or ingredients' %>
      <%= f.submit :style => "display: none;" %>
      <% end %>
  </div>
</div>
